# Antigravity Autonomous Permissions & Execution

- **Default Execution Mode:** Autonomous / Bypass Permissions (`bypassPermissions`).
- Proactively run terminal build commands, generators, typechecks, and linters without waiting for manual confirmation.
- Safe operations (file viewing, code generation, package bundling, testing) are pre-authorized with wildcard allow: `*`.
